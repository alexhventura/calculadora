/** Limite de segurança contra travamento do navegador (~600 anos em meses). */
export const MAX_SIMULATION_MONTHS = 7200;

/** Meses máximos de vínculo empregatício para cálculo automático (500 anos). */
export const MAX_RESCISAO_MESES = 6000;
